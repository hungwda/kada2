/**
 * Onboarding - User onboarding flow (stub)
 */

import { h } from 'preact';

const Onboarding = ({ onComplete }) => {
  // Stub component - implement later
  if (onComplete) onComplete();
  return null;
};

export default Onboarding;
